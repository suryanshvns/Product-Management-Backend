/**
 * Models are defined by Prisma schema (prisma/schema.prisma).
 * Access via: const { prisma } = require('../database/client');
 * e.g. prisma.user, prisma.product, prisma.category, etc.
 */
module.exports = {};
